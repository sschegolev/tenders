import styled from 'styled-components';

export const AvatarManagerContainer = styled.div`
  margin-top: 32px;
  margin-bottom: 28px;
  display: flex;
`;

export const ClearButton = styled.div`
  opacity: .54;
  padding: 22px 5px 5px;
  
  &:hover {
    cursor: pointer;
  }
`;